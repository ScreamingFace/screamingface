# Honest imported grading activity

Imported answer recording must not claim a verdict. Retain the four stage kinds: represent recording as an answering operation with a closed action=recording fact. Suppress the imported attempt-packaging stage. Use explicit grading-call identity for judge model facts, with role=judge. Client decodes both allowlisted facts and numbers events by existing same-run case identity joins. Actual scorer outcomes alone produce case-level grading completion. Keep built-in boards unchanged.

Structured activity defaults to full in Settings, local configuration, observation wiring, worker and Helm; explicit off retains precedence. No prompts, targets or outputs enter logs. Imported scoring still occurs during aggregate after all answers. Aggregation start may precede judge calls; completion follows them.

Reproduce using a real imported board URL4 and fake model routes. Paid owner notebook acceptance remains separate.

## Built-in judges and burst delivery

Built-in rubric boards already register exact judge requests against Engine-owned
Case/Evidence identities for accounting. Activity may reuse that registration only
when the request has one unambiguous Case owner in the current observation run and
is not executing as a Candidate. Explicit Inspect grading scope remains authoritative.
No generated URL4, model prompt, provider parameters, or score semantics change.

A published operation start reserves admission capacity for its eventual terminal
record. This prevents parallel completions from exhausting the producer's shared
reserve and stranding previously visible starts. Downstream delivery remains bounded
and best-effort; bridge loss and history eviction must remain visible, never be
misrepresented as completed calls.

Pagination uses inline Older/Newer controls beside Copy inside the expanded log area,
with disabled boundary buttons and no visible numeric page-input box.

User revision: remove pagination entirely; render all retained operations in the existing scroll panel with Copy.

Timeline revision: calls retain their starting position/time; successful stage summaries use completion time/order. Hide routine recording only, retain errors. Evaluation completion requires a complete scored result.

Wording revision: Calling/Called; explicit judges use Grading with and, only with successful case-grading evidence, Graded with. Full IDs remain in hover/copy; display removes only openrouter/.
