# OME-983 — Candidate failure summary

Approved local scope: show each Candidate's name before its grouped failure lines when a Report
contains multiple Candidates. Group matching stage/code/message only within each Candidate.
Keep the existing single-Candidate presentation, error styling, alert role and raw JSON disclosure.
Case ids are scoped to Candidates; identical ids across Candidates must not become one list.

Approved refinement: use a Candidate heading and indented failure list per Candidate, preserving
existing error wording. Direct Linear description/status updates authorized; no comments.
