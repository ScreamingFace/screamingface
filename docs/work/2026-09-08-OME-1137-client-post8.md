---
ticket: OME-1137
stack: screamingface
status: in_progress
started: 2026-09-08
finished:
---

# OME-1137 — Release the ScreamingFace client as 0.1.1.post8

## Intent

Make the Client changes merged after post7 installable as `0.1.1.post8`, without widening this
manual release bump into source, dependency, or release-automation changes. Publish the result
to PyPI via the `release-screamingface` workflow.

## Planned changes

- `packages/screamingface/pyproject.toml` — version only (`0.1.1.post7` → `0.1.1.post8`).
- `packages/screamingface/uv.lock` — matching local package version.
- This unit's task and work records.

## Test plan

- Manifest and lockfile agree on `0.1.1.post8`.
- `tests/test_version.py` passes: `sf.__version__` (installed distribution metadata) equals the
  `pyproject.toml` version after `uv` re-syncs the editable install.
- Built distribution metadata reports `0.1.1.post8` (`scripts/check_distribution.py`).

## Acceptance

- The installable Client version is `0.1.1.post8`.
- No source, dependency, API, or runtime behavior changes.
- `0.1.1.post8` published to PyPI.

## Outcome

- **Actual files:** `packages/screamingface/pyproject.toml` (version `0.1.1.post7` →
  `0.1.1.post8`), `packages/screamingface/uv.lock` (matching `screamingface` entry), plus this
  task mirror and work ledger. Change set matches PR #806 (post7) exactly; `CHANGELOG.md`
  untouched.
- **Commits:** `chore(screamingface): release the client as 0.1.1.post8` (Refs: OME-1137).
- **Gates:** `uv run .claude/scripts/run_gates.py screamingface` — ALL GATES GREEN (ruff check,
  ruff format --check, pyright, pytest `--cov=screamingface --cov-fail-under=95`,
  check_notebooks, `uv build`, check_distribution). `sf.__version__` reports `0.1.1.post8`; wheel
  + sdist built as `0.1.1.post8`.
- **Deviations:** none to the change set. The fresh worktree venv needed `uv sync --extra
  notebook` (exactly what CI does at `screamingface-tests.yml:48`) before `uv run pyright` could
  resolve `ipywidgets`; environment prep only, no code or gate change.
