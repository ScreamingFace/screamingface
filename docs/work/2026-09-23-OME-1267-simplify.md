---
ticket: OME-1267 (filed at PR time — earlier commits say `Refs: none (owner decision RD4)`)
stack: screamingface-engine
status: done
started: 2026-09-23
finished: 2026-09-23
---

# OME-1267 — /simplify pass over PR #1030 (OME-1267)

## Intent

Quality-only cleanup of the whole PR diff (reuse, simplification, efficiency, altitude). No
behaviour change except one drift fix: local sync requests get the same `origin="sync"` log
binding the deployed node tier already has (FX-6).

## Planned changes

- Batch A (node tier + run path): `job_env.py` (one tolerant number parser),
  `runner/main.py` (use it; one `SharedWorld` param), `world/node_tier/settings.py`
  (defaults read once), `world/node_tier/build.py` (one readiness-fail path),
  `world/node_tier/send.py` (no double error parse; S3 client reuse via store),
  `world/wire.py` (`send_url4_error` via `write_json`; one timeout-hint constant),
  `world/serving.py` (drop unused kwarg, one isinstance), `artifacts/s3.py`.
- Batch B (App side): `rest/forwarder.py` (one retry loop, parse base URL once, module
  header map, `config_path`), `world/config.py` (`config_path(env)`), `rest/artifacts.py`
  (shared clock default), `rest/routes.py` (import `cache_intent`), `request_scope.py`
  (`identity_of` reuse), `world/connector.py` (inline `_post_attempt`, header constant).
- Batch C (local): `request_scope.py` bind helper used by `local.py` and
  `world/node_tier/tier.py`; typed holder in `local.py`; pre-benchmark route set recorded by
  `world/factory.py` replaces the scratch-node probe in `benchmarks/registry.py`;
  `NodeTier` derives `mounts` from its node when not given.
- Batch D (chart/CI): `.github/scripts/verify_chart_wiring.py` (drop duplicate render and
  re-fetch, one `_selects` helper, release constant), helm `nodeSelectorLabels` owns
  `component: node`.

## Test plan

- Refactors only: existing tests are the proof. No test edited or deleted (append-only).
- Drift fix (local `origin="sync"`): new test in a new file.

## Acceptance

- `uv run .claude/scripts/run_gates.py screamingface-engine --skip-append-only` green.
- `python .github/scripts/verify_chart_wiring.py` green; helm render unchanged except
  label order.

## Outcome (fill at the end — required before COMMIT)

- **Actual files:** as planned, plus `auth/__init__.py`, `auth/dependencies.py`,
  `ws/endpoint.py` (one public `default_clock`), `rest/cache_header.py` (docstring), and the
  new `tests/unit/test_local_sync_origin.py`. `artifacts/s3.py` ends unchanged.
- **Commits:** the chart/CI refactor, then the engine refactor (this ledger rides with it).
- **Gates:** `run_gates.py screamingface-engine --skip-append-only` ALL GATES GREEN (no
  existing test edited; one new test file). `verify_chart_wiring.py` green; layering OK.
- **Reviews:** four review lanes (reuse, simplification, efficiency, altitude), then one
  design review (ACCEPT-WITH-FIXES, no structural finding) and one fix round.
- **Deviations:**
  - Reverted after review: S3 client reuse (no App-side owner closes it), the
    `safePodLabels` helm helper (changed the App render), `identity_of` in
    `trace_from_headers` (engine convention is url4's `valid_traceparent`), and the
    `isinstance(Url4Node)` check in `local.py` (broke `test_only_engine_extensions_import_url4`).
  - The pre-benchmark mount set lives in an engine-owned `WeakKeyDictionary` side table
    (`world/factory.direct_mount_paths`), not an attribute on `Url4Node`.
  - Skipped (append-only tests pin the shape): typed holder in `local.py`; single parse of
    the error body in `send.py`.
  - Follow-ups, too large for a cleanup pass: one pure `declared_mount_paths(config)` for all
    three mount-set computations; url4 `ResolutionError` → 502 plus configurable
    retry-after/timeout message (removes `_remap`); `World` as a dataclass carrying its
    section; one carrier for the request origin; S3 client reuse with a real owner.
