# OME-1197 — Plan

1. RED: test generic origin validation/collection; custom gateway detail codes; unmarked errors; protected checker failure; real transport/empty/malformed raise sites.
2. Add the optional ResolutionError field and bounded collection serialization.
3. Stamp Engine connector errors, replace IFEval code-list classifier with origin check; migrate approved three cases to real connector raises.
4. Run URL4 and Engine gates, review additive contract and unchanged successful execution. Open draft referencing existing OME-1197; no merge or Linear comments.
