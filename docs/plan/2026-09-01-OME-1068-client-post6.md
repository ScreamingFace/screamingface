# OME-1068 — Implementation plan: Client 0.1.1.post6

Spec: `docs/spec/2026-09-01-OME-1068-client-post6.md` · Stack: screamingface · Branch:
`OME-1068-bump-screamingface-post6`

## 1. Pin the intended release with a failing check

- Assert that the package manifest and lockfile both report `0.1.1.post6`.
- Run the check before editing package metadata and record the expected failure.

## 2. Bump package metadata

- Change `packages/screamingface/pyproject.toml` from `0.1.1.post5` to `0.1.1.post6`.
- Regenerate `packages/screamingface/uv.lock` so its local package entry matches.

## 3. Verify and publish the change

- Build the wheel and verify its distribution metadata reports `0.1.1.post6`.
- Run `python3 .claude/scripts/run_gates.py screamingface`.
- Complete the ledger, commit with `Refs: OME-1068`, push, and open a draft PR.
- Keep release-please reconciliation explicitly out of this narrow manual bump.
