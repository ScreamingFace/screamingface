"""screamingface-engine backend — the App: mints tokens, upgrades WebSocket
connections, and schedules Runner Jobs (REST + WS control plane).

This temporary source change qualifies the Engine-only Preview path.
"""
