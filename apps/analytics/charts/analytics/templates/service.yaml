apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-analytics
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-analytics
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
