apiVersion: v1
kind: Service
metadata:
  name: {{ include "report-intake.fullname" . }}
  labels:
    {{- include "report-intake.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "report-intake.selectorLabels" . | nindent 4 }}
